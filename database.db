from app import db
db.create_all()

# Add sample products
from models import Product
p1 = Product(name="Apple", category="Fruit", price=0.5, stock=100)
p2 = Product(name="Bread", category="Bakery", price=2.0, stock=50)
db.session.add(p1)
db.session.add(p2)
db.session.commit()
